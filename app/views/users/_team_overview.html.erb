Your team consists of the following members:
<table class="table table-striped table-hover">
  <thead>
  <tr>
    <td>User name</td>
    <td class="text-right-aligned">Profile</td>
  </tr>
  </thead>
  <tbody>
  <% User.members.each do |user| %>
      <tr>
        <td>
          <%= user.name %>
        </td>
        <td class="text-right-aligned">
          <%= link_to 'Show users profile', user_path(user) %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>